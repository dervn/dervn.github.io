# 说明

```
npm install hexo-cli -g #安装hexo

git@github.com:dervn/dervn.github.io.git

cd dervn.github.io

git co src

npm install  #更新package.json中依赖

git submodule init

git submodule update #更新submodule

hexo g #生成HTML文件

hexo s #本地预览

hexo d #远程发布
```
